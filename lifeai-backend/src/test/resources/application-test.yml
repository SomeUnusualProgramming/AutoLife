spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lifeai_test
    username: postgres
    password: postgres
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

logging:
  level:
    root: WARN
    com.lifeai: INFO

server:
  port: 8080
